# receipt-agent
AI-powered receipt &amp; invoice parser. Drop an image → GPT-4o extracts vendor, amount, date, and line items via agentic tool-use loop → auto-appends to Google Sheets. Built with React + Vite.
